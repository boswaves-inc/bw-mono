import type { Config } from 'tailwindcss';

const config = {
    plugins: [
    ],
    theme: {
        // extend: {
        //     aria: {
        //         asc: 'sort="ascending"',
        //         desc: 'sort="descending"',
        //     },
        // },
    },
} satisfies Config;

export default config